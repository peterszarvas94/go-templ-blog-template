root = "."
tmp_dir = "bin"

[build]
  bin = "./bin/ssr"
  cmd = "go build -o ./bin/ssr ./cmd/ssr"
  delay = 100
  exclude_dir = ["bin"]
  include_ext = ["go", "md"]
  stop_on_error = false

[log]
  main_only = true

[misc]
  clean_on_exit = false
